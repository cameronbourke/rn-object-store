{
  "name": "rn-object-store",
  "version": "0.0.4",
  "description": "A lightweight wrapper around React Native's AsyncStorage, which makes working with objects in AsyncStorage a breeze.",
  "main": "dist/index.js",
  "scripts": {
    "compile": "babel src/index.js --out-file dist/index.js",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react-native",
    "storage",
    "async-storage",
    "local-storage",
    "react"
  ],
  "files": [
    "dist"
  ],
  "author": "Cameron Bourke <cameron2295@gmail.com> (http://cameronbourke.com/)",
  "license": "MIT",
  "dependencies": {
    "native-promise-only": "0.x"
  },
  "peerDependencies": {
    "react-native": "0.x"
  },
  "devDependencies": {
    "babel-cli": "6.6.4",
    "babel-core": "6.6.4",
    "babel-preset-es2015": "6.6.0",
    "babel-preset-react": "6.5.0",
    "babel-preset-stage-0": "6.5.0"
  }
}
